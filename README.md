# setup-multiple-github-accounts-in-windows

1. create a .ssh file  if not present in the root directory 

 ```bash
 open command prompt and navigate to root directory using this command
 cd ~
   mkdir .ssh
